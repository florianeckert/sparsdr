id: sparsdr_time_encoded_to_tagged_stream
label: time_encoded_to_tagged_stream
category: '[SparSDR]'

templates:
  imports: import sparsdr
  make: sparsdr.time_encoded_to_tagged_stream()

inputs:
- label: input
  dtype: complex

outputs:
- label: output
  dtype: complex

##  'file_format' specifies the version of the GRC yml format used in the file
##  and should usually not be changed.
file_format: 1
