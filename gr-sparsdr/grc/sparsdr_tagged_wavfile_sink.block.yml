# auto-generated by grc.converter

id: sparsdr_tagged_wavfile_sink
label: tagged_wavfile_sink
category: '[sparsdr]'

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import sparsdr
    make: sparsdr.tagged_wavfile_sink(${directory}, ${sample_rate}, ${bits_per_sample})

file_format: 1
