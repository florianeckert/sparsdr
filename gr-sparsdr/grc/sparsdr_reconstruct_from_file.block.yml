# auto-generated by grc.converter

id: variable_sparsdr_reconstruct_from_file
label: SparSDR Reconstruct From File
category: '[SparSDR]'
flags:
- show_id

parameters:
-   id: reconstruct_path
    label: Executable
    dtype: string
    default: sparsdr_reconstruct
-   id: band_count
    label: Bands
    dtype: int
    default: '1'
    hide: part
-   id: input_path
    label: Input path
    dtype: file_open
-   id: time_config
    label: Time Processing
    dtype: enum
    option_labels: ["None", "Encode in Stream", "Tags", "Pad with Noise"]
    options: ["0", "1", "2", "3"]
    default: 2
-   id: band_0_frequency
    label: Band 0 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 0 else 'all') }
-   id: band_0_bins
    label: Band 0 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 0 else 'all') }
-   id: band_1_frequency
    label: Band 1 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 1 else 'all') }
-   id: band_1_bins
    label: Band 1 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 1 else 'all') }
-   id: band_2_frequency
    label: Band 2 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 2 else 'all') }
-   id: band_2_bins
    label: Band 2 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 2 else 'all') }
-   id: band_3_frequency
    label: Band 3 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 3 else 'all') }
-   id: band_3_bins
    label: Band 3 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 3 else 'all') }
-   id: band_4_frequency
    label: Band 4 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 4 else 'all') }
-   id: band_4_bins
    label: Band 4 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 4 else 'all') }
-   id: band_5_frequency
    label: Band 5 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 5 else 'all') }
-   id: band_5_bins
    label: Band 5 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 5 else 'all') }
-   id: band_6_frequency
    label: Band 6 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 6 else 'all') }
-   id: band_6_bins
    label: Band 6 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 6 else 'all') }
-   id: band_7_frequency
    label: Band 7 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 7 else 'all') }
-   id: band_7_bins
    label: Band 7 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 7 else 'all') }
-   id: band_8_frequency
    label: Band 8 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 8 else 'all') }
-   id: band_8_bins
    label: Band 8 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 8 else 'all') }
-   id: band_9_frequency
    label: Band 9 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 9 else 'all') }
-   id: band_9_bins
    label: Band 9 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 9 else 'all') }
-   id: band_10_frequency
    label: Band 10 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 10 else 'all') }
-   id: band_10_bins
    label: Band 10 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 10 else 'all') }
-   id: band_11_frequency
    label: Band 11 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 11 else 'all') }
-   id: band_11_bins
    label: Band 11 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 11 else 'all') }
-   id: band_12_frequency
    label: Band 12 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 12 else 'all') }
-   id: band_12_bins
    label: Band 12 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 12 else 'all') }
-   id: band_13_frequency
    label: Band 13 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 13 else 'all') }
-   id: band_13_bins
    label: Band 13 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 13 else 'all') }
-   id: band_14_frequency
    label: Band 14 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 14 else 'all') }
-   id: band_14_bins
    label: Band 14 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 14 else 'all') }
-   id: band_15_frequency
    label: Band 15 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 15 else 'all') }
-   id: band_15_bins
    label: Band 15 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 15 else 'all') }
-   id: band_16_frequency
    label: Band 16 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 16 else 'all') }
-   id: band_16_bins
    label: Band 16 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 16 else 'all') }
-   id: band_17_frequency
    label: Band 17 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 17 else 'all') }
-   id: band_17_bins
    label: Band 17 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 17 else 'all') }
-   id: band_18_frequency
    label: Band 18 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 18 else 'all') }
-   id: band_18_bins
    label: Band 18 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 18 else 'all') }
-   id: band_19_frequency
    label: Band 19 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 19 else 'all') }
-   id: band_19_bins
    label: Band 19 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 19 else 'all') }
-   id: band_20_frequency
    label: Band 20 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 20 else 'all') }
-   id: band_20_bins
    label: Band 20 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 20 else 'all') }
-   id: band_21_frequency
    label: Band 21 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 21 else 'all') }
-   id: band_21_bins
    label: Band 21 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 21 else 'all') }
-   id: band_22_frequency
    label: Band 22 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 22 else 'all') }
-   id: band_22_bins
    label: Band 22 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 22 else 'all') }
-   id: band_23_frequency
    label: Band 23 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 23 else 'all') }
-   id: band_23_bins
    label: Band 23 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 23 else 'all') }
-   id: band_24_frequency
    label: Band 24 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 24 else 'all') }
-   id: band_24_bins
    label: Band 24 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 24 else 'all') }
-   id: band_25_frequency
    label: Band 25 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 25 else 'all') }
-   id: band_25_bins
    label: Band 25 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 25 else 'all') }
-   id: band_26_frequency
    label: Band 26 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 26 else 'all') }
-   id: band_26_bins
    label: Band 26 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 26 else 'all') }
-   id: band_27_frequency
    label: Band 27 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 27 else 'all') }
-   id: band_27_bins
    label: Band 27 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 27 else 'all') }
-   id: band_28_frequency
    label: Band 28 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 28 else 'all') }
-   id: band_28_bins
    label: Band 28 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 28 else 'all') }
-   id: band_29_frequency
    label: Band 29 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 29 else 'all') }
-   id: band_29_bins
    label: Band 29 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 29 else 'all') }
-   id: band_30_frequency
    label: Band 30 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 30 else 'all') }
-   id: band_30_bins
    label: Band 30 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 30 else 'all') }
-   id: band_31_frequency
    label: Band 31 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 31 else 'all') }
-   id: band_31_bins
    label: Band 31 bins
    category: Bands
    dtype: int
    default: '2048'
    hide: ${ ('none' if band_count > 31 else 'all') }
-   id: band_32_frequency
    label: Band 32 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 32 else 'all') }
-   id: band_32_bins
    label: Band 32 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 32 else 'all') }
-   id: band_33_frequency
    label: Band 33 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 33 else 'all') }
-   id: band_33_bins
    label: Band 33 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 33 else 'all') }
-   id: band_34_frequency
    label: Band 34 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 34 else 'all') }
-   id: band_34_bins
    label: Band 34 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 34 else 'all') }
-   id: band_35_frequency
    label: Band 35 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 35 else 'all') }
-   id: band_35_bins
    label: Band 35 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 35 else 'all') }
-   id: band_36_frequency
    label: Band 36 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 36 else 'all') }
-   id: band_36_bins
    label: Band 36 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 36 else 'all') }
-   id: band_37_frequency
    label: Band 37 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 37 else 'all') }
-   id: band_37_bins
    label: Band 37 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 37 else 'all') }
-   id: band_38_frequency
    label: Band 38 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 38 else 'all') }
-   id: band_38_bins
    label: Band 38 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 38 else 'all') }
-   id: band_39_frequency
    label: Band 39 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 39 else 'all') }
-   id: band_39_bins
    label: Band 39 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 39 else 'all') }
-   id: band_40_frequency
    label: Band 40 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 40 else 'all') }
-   id: band_40_bins
    label: Band 40 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 40 else 'all') }
-   id: band_41_frequency
    label: Band 41 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 41 else 'all') }
-   id: band_41_bins
    label: Band 41 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 41 else 'all') }
-   id: band_42_frequency
    label: Band 42 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 42 else 'all') }
-   id: band_42_bins
    label: Band 42 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 42 else 'all') }
-   id: band_43_frequency
    label: Band 43 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 43 else 'all') }
-   id: band_43_bins
    label: Band 43 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 43 else 'all') }
-   id: band_44_frequency
    label: Band 44 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 44 else 'all') }
-   id: band_44_bins
    label: Band 44 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 44 else 'all') }
-   id: band_45_frequency
    label: Band 45 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 45 else 'all') }
-   id: band_45_bins
    label: Band 45 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 45 else 'all') }
-   id: band_46_frequency
    label: Band 46 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 46 else 'all') }
-   id: band_46_bins
    label: Band 46 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 46 else 'all') }
-   id: band_47_frequency
    label: Band 47 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 47 else 'all') }
-   id: band_47_bins
    label: Band 47 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 47 else 'all') }
-   id: band_48_frequency
    label: Band 48 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 48 else 'all') }
-   id: band_48_bins
    label: Band 48 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 48 else 'all') }
-   id: band_49_frequency
    label: Band 49 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 49 else 'all') }
-   id: band_49_bins
    label: Band 49 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 49 else 'all') }
-   id: band_50_frequency
    label: Band 50 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 50 else 'all') }
-   id: band_50_bins
    label: Band 50 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 50 else 'all') }
-   id: band_51_frequency
    label: Band 51 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 51 else 'all') }
-   id: band_51_bins
    label: Band 51 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 51 else 'all') }
-   id: band_52_frequency
    label: Band 52 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 52 else 'all') }
-   id: band_52_bins
    label: Band 52 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 52 else 'all') }
-   id: band_53_frequency
    label: Band 53 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 53 else 'all') }
-   id: band_53_bins
    label: Band 53 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 53 else 'all') }
-   id: band_54_frequency
    label: Band 54 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 54 else 'all') }
-   id: band_54_bins
    label: Band 54 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 54 else 'all') }
-   id: band_55_frequency
    label: Band 55 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 55 else 'all') }
-   id: band_55_bins
    label: Band 55 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 55 else 'all') }
-   id: band_56_frequency
    label: Band 56 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 56 else 'all') }
-   id: band_56_bins
    label: Band 56 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 56 else 'all') }
-   id: band_57_frequency
    label: Band 57 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 57 else 'all') }
-   id: band_57_bins
    label: Band 57 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 57 else 'all') }
-   id: band_58_frequency
    label: Band 58 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 58 else 'all') }
-   id: band_58_bins
    label: Band 58 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 58 else 'all') }
-   id: band_59_frequency
    label: Band 59 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 59 else 'all') }
-   id: band_59_bins
    label: Band 59 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 59 else 'all') }
-   id: band_60_frequency
    label: Band 60 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 60 else 'all') }
-   id: band_60_bins
    label: Band 60 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 60 else 'all') }
-   id: band_61_frequency
    label: Band 61 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 61 else 'all') }
-   id: band_61_bins
    label: Band 61 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 61 else 'all') }
-   id: band_62_frequency
    label: Band 62 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 62 else 'all') }
-   id: band_62_bins
    label: Band 62 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 62 else 'all') }
-   id: band_63_frequency
    label: Band 63 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 63 else 'all') }
-   id: band_63_bins
    label: Band 63 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 63 else 'all') }
-   id: band_64_frequency
    label: Band 64 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 64 else 'all') }
-   id: band_64_bins
    label: Band 64 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 64 else 'all') }
-   id: band_65_frequency
    label: Band 65 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 65 else 'all') }
-   id: band_65_bins
    label: Band 65 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 65 else 'all') }
-   id: band_66_frequency
    label: Band 66 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 66 else 'all') }
-   id: band_66_bins
    label: Band 66 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 66 else 'all') }
-   id: band_67_frequency
    label: Band 67 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 67 else 'all') }
-   id: band_67_bins
    label: Band 67 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 67 else 'all') }
-   id: band_68_frequency
    label: Band 68 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 68 else 'all') }
-   id: band_68_bins
    label: Band 68 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 68 else 'all') }
-   id: band_69_frequency
    label: Band 69 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 69 else 'all') }
-   id: band_69_bins
    label: Band 69 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 69 else 'all') }
-   id: band_70_frequency
    label: Band 70 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 70 else 'all') }
-   id: band_70_bins
    label: Band 70 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 70 else 'all') }
-   id: band_71_frequency
    label: Band 71 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 71 else 'all') }
-   id: band_71_bins
    label: Band 71 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 71 else 'all') }
-   id: band_72_frequency
    label: Band 72 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 72 else 'all') }
-   id: band_72_bins
    label: Band 72 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 72 else 'all') }
-   id: band_73_frequency
    label: Band 73 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 73 else 'all') }
-   id: band_73_bins
    label: Band 73 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 73 else 'all') }
-   id: band_74_frequency
    label: Band 74 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 74 else 'all') }
-   id: band_74_bins
    label: Band 74 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 74 else 'all') }
-   id: band_75_frequency
    label: Band 75 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 75 else 'all') }
-   id: band_75_bins
    label: Band 75 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 75 else 'all') }
-   id: band_76_frequency
    label: Band 76 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 76 else 'all') }
-   id: band_76_bins
    label: Band 76 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 76 else 'all') }
-   id: band_77_frequency
    label: Band 77 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 77 else 'all') }
-   id: band_77_bins
    label: Band 77 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 77 else 'all') }
-   id: band_78_frequency
    label: Band 78 frequency
    category: Bands
    dtype: real
    default: '0.0'
    hide: ${ ('none' if band_count > 78 else 'all') }
-   id: band_78_bins
    label: Band 78 bins
    category: Bands
    dtype: int 
    default: '2048'
    hide: ${ ('none' if band_count > 78 else 'all') }

outputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ band_count }
value: ${ value }
asserts:
- ${ 78 >= band_count }
- ${ band_count > 0 }

templates:
    imports: |-
        import sparsdr
        import distutils.spawn
    var_make: |-
        ${id}_bands = sparsdr.band_spec_vector()
        % if int(band_count) > 0:
        ${id}_bands.push_back(sparsdr.band_spec(${band_0_frequency}, ${band_0_bins}))
        % endif
        % if int(band_count) > 1:
        ${id}_bands.push_back(sparsdr.band_spec(${band_1_frequency}, ${band_1_bins}))
        % endif
        % if int(band_count) > 2:
        ${id}_bands.push_back(sparsdr.band_spec(${band_2_frequency}, ${band_2_bins}))
        % endif
        % if int(band_count) > 3:
        ${id}_bands.push_back(sparsdr.band_spec(${band_3_frequency}, ${band_3_bins}))
        % endif
        % if int(band_count) > 4:
        ${id}_bands.push_back(sparsdr.band_spec(${band_4_frequency}, ${band_4_bins}))
        % endif
        % if int(band_count) > 5:
        ${id}_bands.push_back(sparsdr.band_spec(${band_5_frequency}, ${band_5_bins}))
        % endif
        % if int(band_count) > 6:
        ${id}_bands.push_back(sparsdr.band_spec(${band_6_frequency}, ${band_6_bins}))
        % endif
        % if int(band_count) > 7:
        ${id}_bands.push_back(sparsdr.band_spec(${band_7_frequency}, ${band_7_bins}))
        % endif
        % if int(band_count) > 8:
        ${id}_bands.push_back(sparsdr.band_spec(${band_8_frequency}, ${band_8_bins}))
        % endif
        % if int(band_count) > 9:
        ${id}_bands.push_back(sparsdr.band_spec(${band_9_frequency}, ${band_9_bins}))
        % endif
        % if int(band_count) > 10:
        ${id}_bands.push_back(sparsdr.band_spec(${band_10_frequency}, ${band_10_bins}))
        % endif
        % if int(band_count) > 11:
        ${id}_bands.push_back(sparsdr.band_spec(${band_11_frequency}, ${band_11_bins}))
        % endif
        % if int(band_count) > 12:
        ${id}_bands.push_back(sparsdr.band_spec(${band_12_frequency}, ${band_12_bins}))
        % endif
        % if int(band_count) > 13:
        ${id}_bands.push_back(sparsdr.band_spec(${band_13_frequency}, ${band_13_bins}))
        % endif
        % if int(band_count) > 14:
        ${id}_bands.push_back(sparsdr.band_spec(${band_14_frequency}, ${band_14_bins}))
        % endif
        % if int(band_count) > 15:
        ${id}_bands.push_back(sparsdr.band_spec(${band_15_frequency}, ${band_15_bins}))
        % endif
        % if int(band_count) > 16:
        ${id}_bands.push_back(sparsdr.band_spec(${band_16_frequency}, ${band_16_bins}))
        % endif
        % if int(band_count) > 17:
        ${id}_bands.push_back(sparsdr.band_spec(${band_17_frequency}, ${band_17_bins}))
        % endif
        % if int(band_count) > 18:
        ${id}_bands.push_back(sparsdr.band_spec(${band_18_frequency}, ${band_18_bins}))
        % endif
        % if int(band_count) > 19:
        ${id}_bands.push_back(sparsdr.band_spec(${band_19_frequency}, ${band_19_bins}))
        % endif
        % if int(band_count) > 20:
        ${id}_bands.push_back(sparsdr.band_spec(${band_20_frequency}, ${band_20_bins}))
        % endif
        % if int(band_count) > 21:
        ${id}_bands.push_back(sparsdr.band_spec(${band_21_frequency}, ${band_21_bins}))
        % endif
        % if int(band_count) > 22:
        ${id}_bands.push_back(sparsdr.band_spec(${band_22_frequency}, ${band_22_bins}))
        % endif
        % if int(band_count) > 23:
        ${id}_bands.push_back(sparsdr.band_spec(${band_23_frequency}, ${band_23_bins}))
        % endif
        % if int(band_count) > 24:
        ${id}_bands.push_back(sparsdr.band_spec(${band_24_frequency}, ${band_24_bins}))
        % endif
        % if int(band_count) > 25:
        ${id}_bands.push_back(sparsdr.band_spec(${band_25_frequency}, ${band_25_bins}))
        % endif
        % if int(band_count) > 26:
        ${id}_bands.push_back(sparsdr.band_spec(${band_26_frequency}, ${band_26_bins}))
        % endif
        % if int(band_count) > 27:
        ${id}_bands.push_back(sparsdr.band_spec(${band_27_frequency}, ${band_27_bins}))
        % endif
        % if int(band_count) > 28:
        ${id}_bands.push_back(sparsdr.band_spec(${band_28_frequency}, ${band_28_bins}))
        % endif
        % if int(band_count) > 29:
        ${id}_bands.push_back(sparsdr.band_spec(${band_29_frequency}, ${band_29_bins}))
        % endif
        % if int(band_count) > 30:
        ${id}_bands.push_back(sparsdr.band_spec(${band_30_frequency}, ${band_30_bins}))
        % endif
        % if int(band_count) > 31:
        ${id}_bands.push_back(sparsdr.band_spec(${band_31_frequency}, ${band_31_bins}))
        % endif
        % if int(band_count) > 32:
        ${id}_bands.push_back(sparsdr.band_spec(${band_32_frequency}, ${band_32_bins}))
        % endif
        % if int(band_count) > 33:
        ${id}_bands.push_back(sparsdr.band_spec(${band_33_frequency}, ${band_33_bins}))
        % endif
        % if int(band_count) > 34:
        ${id}_bands.push_back(sparsdr.band_spec(${band_34_frequency}, ${band_34_bins}))
        % endif
        % if int(band_count) > 35:
        ${id}_bands.push_back(sparsdr.band_spec(${band_35_frequency}, ${band_35_bins}))
        % endif
        % if int(band_count) > 36:
        ${id}_bands.push_back(sparsdr.band_spec(${band_36_frequency}, ${band_36_bins}))
        % endif
        % if int(band_count) > 37:
        ${id}_bands.push_back(sparsdr.band_spec(${band_37_frequency}, ${band_37_bins}))
        % endif
        % if int(band_count) > 38:
        ${id}_bands.push_back(sparsdr.band_spec(${band_38_frequency}, ${band_38_bins}))
        % endif
        % if int(band_count) > 39:
        ${id}_bands.push_back(sparsdr.band_spec(${band_39_frequency}, ${band_39_bins}))
        % endif
        % if int(band_count) > 40:
        ${id}_bands.push_back(sparsdr.band_spec(${band_40_frequency}, ${band_40_bins}))
        % endif
        % if int(band_count) > 41:
        ${id}_bands.push_back(sparsdr.band_spec(${band_41_frequency}, ${band_41_bins}))
        % endif
        % if int(band_count) > 42:
        ${id}_bands.push_back(sparsdr.band_spec(${band_42_frequency}, ${band_42_bins}))
        % endif
        % if int(band_count) > 43:
        ${id}_bands.push_back(sparsdr.band_spec(${band_43_frequency}, ${band_43_bins}))
        % endif
        % if int(band_count) > 44:
        ${id}_bands.push_back(sparsdr.band_spec(${band_44_frequency}, ${band_44_bins}))
        % endif
        % if int(band_count) > 45:
        ${id}_bands.push_back(sparsdr.band_spec(${band_45_frequency}, ${band_45_bins}))
        % endif
        % if int(band_count) > 46:
        ${id}_bands.push_back(sparsdr.band_spec(${band_46_frequency}, ${band_46_bins}))
        % endif
        % if int(band_count) > 47:
        ${id}_bands.push_back(sparsdr.band_spec(${band_47_frequency}, ${band_47_bins}))
        % endif
        % if int(band_count) > 48:
        ${id}_bands.push_back(sparsdr.band_spec(${band_48_frequency}, ${band_48_bins}))
        % endif
        % if int(band_count) > 49:
        ${id}_bands.push_back(sparsdr.band_spec(${band_49_frequency}, ${band_49_bins}))
        % endif
        % if int(band_count) > 50:
        ${id}_bands.push_back(sparsdr.band_spec(${band_50_frequency}, ${band_50_bins}))
        % endif
        % if int(band_count) > 51:
        ${id}_bands.push_back(sparsdr.band_spec(${band_51_frequency}, ${band_51_bins}))
        % endif
        % if int(band_count) > 52:
        ${id}_bands.push_back(sparsdr.band_spec(${band_52_frequency}, ${band_52_bins}))
        % endif
        % if int(band_count) > 53:
        ${id}_bands.push_back(sparsdr.band_spec(${band_53_frequency}, ${band_53_bins}))
        % endif
        % if int(band_count) > 54:
        ${id}_bands.push_back(sparsdr.band_spec(${band_54_frequency}, ${band_54_bins}))
        % endif
        % if int(band_count) > 55:
        ${id}_bands.push_back(sparsdr.band_spec(${band_55_frequency}, ${band_55_bins}))
        % endif
        % if int(band_count) > 56:
        ${id}_bands.push_back(sparsdr.band_spec(${band_56_frequency}, ${band_56_bins}))
        % endif
        % if int(band_count) > 57:
        ${id}_bands.push_back(sparsdr.band_spec(${band_57_frequency}, ${band_57_bins}))
        % endif
        % if int(band_count) > 58:
        ${id}_bands.push_back(sparsdr.band_spec(${band_58_frequency}, ${band_58_bins}))
        % endif
        % if int(band_count) > 59:
        ${id}_bands.push_back(sparsdr.band_spec(${band_59_frequency}, ${band_59_bins}))
        % endif
        % if int(band_count) > 60:
        ${id}_bands.push_back(sparsdr.band_spec(${band_60_frequency}, ${band_60_bins}))
        % endif
        % if int(band_count) > 61:
        ${id}_bands.push_back(sparsdr.band_spec(${band_61_frequency}, ${band_61_bins}))
        % endif
        % if int(band_count) > 62:
        ${id}_bands.push_back(sparsdr.band_spec(${band_62_frequency}, ${band_62_bins}))
        % endif
        % if int(band_count) > 63:
        ${id}_bands.push_back(sparsdr.band_spec(${band_63_frequency}, ${band_63_bins}))
        % endif
        % if int(band_count) > 64:
        ${id}_bands.push_back(sparsdr.band_spec(${band_64_frequency}, ${band_64_bins}))
        % endif
        % if int(band_count) > 65:
        ${id}_bands.push_back(sparsdr.band_spec(${band_65_frequency}, ${band_65_bins}))
        % endif
        % if int(band_count) > 66:
        ${id}_bands.push_back(sparsdr.band_spec(${band_66_frequency}, ${band_66_bins}))
        % endif
        % if int(band_count) > 67:
        ${id}_bands.push_back(sparsdr.band_spec(${band_67_frequency}, ${band_67_bins}))
        % endif
        % if int(band_count) > 68:
        ${id}_bands.push_back(sparsdr.band_spec(${band_68_frequency}, ${band_68_bins}))
        % endif
        % if int(band_count) > 69:
        ${id}_bands.push_back(sparsdr.band_spec(${band_69_frequency}, ${band_69_bins}))
        % endif
        % if int(band_count) > 70:
        ${id}_bands.push_back(sparsdr.band_spec(${band_70_frequency}, ${band_70_bins}))
        % endif
        % if int(band_count) > 71:
        ${id}_bands.push_back(sparsdr.band_spec(${band_71_frequency}, ${band_71_bins}))
        % endif
        % if int(band_count) > 72:
        ${id}_bands.push_back(sparsdr.band_spec(${band_72_frequency}, ${band_72_bins}))
        % endif
        % if int(band_count) > 73:
        ${id}_bands.push_back(sparsdr.band_spec(${band_73_frequency}, ${band_73_bins}))
        % endif
        % if int(band_count) > 74:
        ${id}_bands.push_back(sparsdr.band_spec(${band_74_frequency}, ${band_74_bins}))
        % endif
        % if int(band_count) > 75:
        ${id}_bands.push_back(sparsdr.band_spec(${band_75_frequency}, ${band_75_bins}))
        % endif
        % if int(band_count) > 76:
        ${id}_bands.push_back(sparsdr.band_spec(${band_76_frequency}, ${band_76_bins}))
        % endif
        % if int(band_count) > 77:
        ${id}_bands.push_back(sparsdr.band_spec(${band_77_frequency}, ${band_77_bins}))
        % endif
        % if int(band_count) > 78:
        ${id}_bands.push_back(sparsdr.band_spec(${band_78_frequency}, ${band_78_bins}))
        % endif
        self.${id} = ${id} = sparsdr.reconstruct_from_file(bands=${id}_bands, input_path=${input_path}, reconstruct_path=distutils.spawn.find_executable(${reconstruct_path}), time_config=${time_config})
        

documentation: |-
    The SparSDR reconstruct block reads compressed samples from a file and reconstructs signals from one or more bands.

    Parameters:

    bands: The number of bands to reconstruct

    Time Processing: How to process time information when jumps in time occur due to the samples being below the threshold. None: ignore jumps, Encode in Stream: write timestamps as uint32_t samples preceeded by 0xffffffff, Tags: include tags at regular intervals, Pad with Noise: pad jumps in time with low amplitude noise

    Band parameters for band i:

    Band i frequency: The center frequency to reconstruct, in herz relative to the center frequency used to capture the signals

    Band i bins: The number of bins to use when reconstructing. This determines the bandwidth to reconstruct and the sample rate of the resulting signal.

    Executable: The path to the sparsdr_reconstruct executable. If this is not an absolute path, the block will search for an executable with the correct name in the paths defined by the PATH environment variable.

file_format: 1
